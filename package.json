{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .spago",
    "build": "eslint && spago build --strict",
    "test": "spago test"
  },
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "eslint": "^7.32.0",
    "globals": "^16.0.0",
    "rimraf": "^3.0.2"
  }
}
