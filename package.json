{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "build": "jshint src && jscs src && pulp build"
  },
  "devDependencies": {
    "jscs": "^1.13.1",
    "jshint": "^2.9.1-rc1",
    "pulp": "^6.0.0"
  }
}
